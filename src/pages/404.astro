---
import Layout from '@/layouts/Layout.astro';
import Marquee from '@/components/Marquee';
---
<Layout title="404 — Your Name">
  <section class="not-found section-pad">
    <h1>This page got lost in the void.</h1>
    <p>It doesn't exist, or no longer does.</p>
    <a href="/" class="back-link">← Back to home</a>
  </section>
  <Marquee client:visible text="— PAGE NOT FOUND — SIGNAL LOST — ERROR 404 — " />
</Layout>

<style>
  .not-found {
    min-height: 80vh;
    display: flex;
    flex-direction: column;
    align-items: center;
    justify-content: center;
    text-align: center;
  }
  h1 {
    font-family: var(--font-display);
    /* Druk Wide ships 700/900 only (see fonts.css); a 400 declaration here
       silently resolves to the 700 file anyway, so say what actually renders. */
    font-weight: 700;
    font-size: clamp(1.8rem, 5vw, 3rem);
    max-width: 20ch;
  }
  p {
    color: var(--fg-dim);
    margin-bottom: 2rem;
  }
  .back-link {
    text-transform: uppercase;
    font-size: 0.85rem;
    letter-spacing: 0.05em;
    border-bottom: 1px solid var(--fg);
  }
</style>
